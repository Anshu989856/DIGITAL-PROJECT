import ScrollIndicator from '../ScrollIndicator';

export default function ScrollIndicatorExample() {
  return (
    <div className="relative h-screen bg-background flex items-center justify-center">
      <ScrollIndicator />
    </div>
  );
}
